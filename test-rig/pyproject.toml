[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bark-detector-test-rig"
version = "0.1.0"
description = "Dataset downloader dependencies for the bark-detector test rig"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "yt-dlp>=2024.1",
]

[tool.setuptools]
py-modules = []
