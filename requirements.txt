# TFLite interpreter for the audio classification model. Official Google
# package (successor to tflite_runtime), actively maintained, with prebuilt
# wheels for Python 3.14 on both linux/amd64 and linux/arm64 - unlike the
# third-party wheels this project used to depend on (frigate-nvr/TFlite-builds,
# feranick/TFlite-builds), which topped out at cp311 and forced numpy<2.
ai-edge-litert>=2.0,<3

numpy>=1.26
PyYAML>=6.0
paho-mqtt>=2.1,<3
Flask>=3.0,<4
